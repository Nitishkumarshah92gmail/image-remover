package com.example.nitishgallery.data

/**
 * Options for sorting the image gallery.
 */
enum class SortOption {
    /** Sort by date added (newest first). */
    DATE,
    /** Sort alphabetically by name (A-Z). */
    NAME,
    /** Sort by file size (largest first). */
    SIZE
}

